<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Sign in</title>
  <style>
    :root{
      --bg: #141414; --card:#212121; --muted:#9aa4b2; --text:#e6e8eb;
      --accent:#2563eb; --accent-fg:#fff; --border:#1f2937; --danger:#ef4444;
      --ring: 0 10px 30px rgba(0,0,0,.35);
    }
    @media (prefers-color-scheme: light){
      :root{
        --bg:#f6f8fc; --card:#ffffff; --muted:#5b6472; --text:#0e1116;
        --accent:#2563eb; --accent-fg:#fff; --border:#e5e7eb; --ring:0 12px 30px rgba(37,99,235,.15);
      }
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Apple Color Emoji","Segoe UI Emoji";
      background: radial-gradient(1200px 800px at 10% -10%, rgba(37,99,235,.15), transparent 60%),
                  radial-gradient(1000px 600px at 110% 110%, rgba(147,51,234,.12), transparent 60%),
                  var(--bg);
      color:var(--text); display:grid; place-items:center; padding:24px;
    }
    .card{
      width:100%; max-width:420px; background:var(--card); border:1px solid var(--border);
      border-radius:20px; padding:28px; box-shadow: var(--ring);
    }
    .title{margin:0 0 4px; font-size:26px; letter-spacing:.2px}
    .sub{margin:0 0 18px; color:var(--muted); font-size:14px}
    .btn{
      appearance:none; border:1px solid var(--border); background:#0f141a0a;
      width:100%; border-radius:12px; padding:12px 14px; font-weight:600; cursor:pointer;
      display:flex; align-items:center; justify-content:center; gap:10px; text-decoration:none; color:var(--text);
      transition: transform .06s ease, background .2s ease, border-color .2s ease;
    }
    .btn:hover{background:#0f141a12}
    .btn:active{transform:translateY(1px)}
    .btn-google svg{width:18px; height:18px}
    .or{display:flex; align-items:center; gap:12px; margin:16px 0}
    .or::before,.or::after{content:""; height:1px; background:var(--border); flex:1}
    form{display:grid; gap:12px}
    label{font-size:12px; color:var(--muted)}
    .input{
      margin-top:6px; width:100%; padding:12px 12px; border-radius:12px; border:1px solid var(--border); background:transparent; color:var(--text);
      outline:none; transition:border-color .2s ease, box-shadow .2s ease;
    }
    .input:focus{border-color:var(--accent); box-shadow:0 0 0 3px rgba(37,99,235,.15)}
    .row{display:flex; justify-content:space-between; align-items:center; gap:12px; margin-top:2px}
    .muted-link{font-size:12px; color:var(--accent); text-decoration:none}
    .muted-link:hover{text-decoration:underline}
    .submit{
      background:var(--accent); color:var(--accent-fg); border:0; border-radius:12px; padding:12px 14px; width:100%;
      font-weight:700; cursor:pointer; transition:filter .2s ease, transform .06s ease;
    }
    .submit:hover{filter:brightness(.98)}
    .submit:active{transform:translateY(1px)}
    .error, .success{
      border-radius:12px; padding:10px 12px; font-size:14px; margin-bottom:10px; border:1px solid;
    }
    .error{background:rgba(239,68,68,.08); color:#ef4444; border-color:rgba(239,68,68,.35)}
    .success{background:rgba(34,197,94,.1); color:#10b981; border-color:rgba(16,185,129,.4)}
     
  </style>
</head>
<body>
  <main class="card" role="main" aria-labelledby="title">
    <h1 class="title" id="title">Sign in</h1>
    <p class="sub">Welcome back! Choose a method to continue.</p>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error" role="alert"><%= error %></div>
    <% } %>
    <% if (typeof success !== 'undefined' && success) { %>
      <div class="success" role="status"><%= success %></div>
    <% } %>

    <!-- Google Sign in -->
    <a class="btn btn-google" href="/auth/login/federated/google" aria-label="Sign in with Google">


      <!-- Google 'G' icon -->
      <svg viewBox="0 0 48 48" aria-hidden="true">
        <path fill="#FFC107" d="M43.6 20.5H42V20H24v8h11.3C33.8 31.9 29.3 35 24 35c-6.6 0-12-5.4-12-12S17.4 11 24 11c3 0 5.7 1.1 7.8 3l5.7-5.7C33.7 5.1 29.1 3 24 3 12.4 3 3 12.4 3 24s9.4 21 21 21c10.5 0 19.4-7.6 20.6-17.5.2-1.2.4-2.5.4-3.5 0-1.2-.1-2.4-.4-3.5z"/>
        <path fill="#FF3D00" d="M6.3 14.7l6.6 4.8C14.6 15.6 18.9 13 24 13c3 0 5.7 1.1 7.8 3l5.7-5.7C33.7 5.1 29.1 3 24 3 15 3 7.4 8.2 6.3 14.7z"/>
        <path fill="#4CAF50" d="M24 45c5.2 0 9.9-2 13.4-5.2l-6.2-5.1C29 36.8 26.6 38 24 38c-5.2 0-9.6-3.3-11.2-7.9l-6.6 5C7.4 39.9 15 45 24 45z"/>
        <path fill="#1976D2" d="M43.6 20.5H42V20H24v8h11.3c-1.3 3.9-5.8 7-11.3 7-5.2 0-9.6-3.3-11.2-7.9l-6.6 5C7.4 39.9 15 45 24 45c10.5 0 19.4-7.6 20.6-17.5.2-1.2.4-2.5.4-3.5 0-1.2-.1-2.4-.4-3.5z"/>
      </svg>
      Sign in with Google
    </a>
    <p class="foot">New here? <a class="muted-link" href="/auth/login/federated/google">Create an account</a></p>
  </main>

</body>
</html>
